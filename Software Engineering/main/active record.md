special case of [[Software Engineering/main/data transfer objects]] - public variables, but have some navigational methods like `save` and `find`.

typically they are direct translations from database tables.